# Non-secret runtime config for project resybot-bd2db, loaded by Firebase on deploy.
# Cancellation watch: "true" books assigned slots on the watcher's own Resy account;
# anything else keeps the watch in shadow mode (log only).
WATCH_BOOKING_ENABLED=true
